import { ClickDoubleDirective } from './click-double.directive';

describe('ClickDoubleDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickDoubleDirective();
    expect(directive).toBeTruthy();
  });
});
